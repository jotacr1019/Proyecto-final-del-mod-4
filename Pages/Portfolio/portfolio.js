function main() {
    const contenedorHeader = document.querySelector(".contenedor-header");
    addHeader(contenedorHeader);

    const contenedorFooter = document.querySelector(".contenedor-footer");
    addFooter(contenedorFooter);
}

main();